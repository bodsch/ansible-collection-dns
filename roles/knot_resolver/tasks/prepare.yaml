---

- name: include OS specific configuration
  ansible.builtin.include_vars: "{{ lookup('first_found', params) }}"
  vars:
    params:
      paths:
        - "vars"
      files:
        # eg. debian-10 / ubuntu-20.04 / centos-8 / oraclelinux-8
        - "{{ ansible_facts.distribution | lower }}-{{ ansible_facts.distribution_major_version }}.yaml"
        # eg. archlinux-systemd / archlinux-openrc
        - "{{ ansible_facts.distribution | lower }}-{{ ansible_facts.service_mgr | lower }}.yaml"
        # eg. debian / ubuntu / centos / oraclelinux
        - "{{ ansible_facts.distribution | lower }}.yaml"
        # eg. redhat / debian / archlinux
        - "{{ ansible_facts.os_family | lower }}.yaml"
        # artixlinux
        - "{{ ansible_facts.os_family | lower | replace(' ', '') }}.yaml"
        - default.yaml
      skip: true

- name: install dedendency
  ansible.builtin.package:
    name: "{{ knot_resolver_dependencies }}"
    state: present
  when:
    - knot_resolver_dependencies is defined
    - knot_resolver_dependencies | count > 0

# - name: get latest system information
  # ansible.builtin.setup:

...
